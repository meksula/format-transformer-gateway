spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: format-transformer
          uri: http://format-transformer
          predicates:
          - Path=/api/v1/transformation, /api/v1/activity
        - id: format-transformer-stats
          uri: http://format-transformer-stats
          predicates:
          - Path=/api/v1/stats
server:
  port: 8080
